# OPC 团队协作仪表盘 — 产品方案

## 1. 产品概述

**目标**：一个浏览器可直接打开的纯静态 HTML 页面，可视化展示 OPC 四人团队（Bojack/Athena/Mercury/Codex）的实时协作状态。

**核心价值**：一眼看清「谁在干什么、任务到哪了、刚才发生了什么」。

**风格基准**：继承 Share space dashboard 的暗色主题和纯静态架构。

## 2. UI 布局（三区制）

```
┌─────────────────────────────────────────────────────────┐
│  🎯 OPC TEAM DASHBOARD                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │
│  │ 🎯Bojack │ │ 🔬Athena │ │ ✍️Mercury│ │ 🔨Codex  │   │
│  │ 协调员   │ │ 研究员   │ │ 写手     │ │ 构建者   │   │
│  │ ● online │ │ ◉ working│ │ ○ idle   │ │ ◉ working│   │
│  │ 审查方案 │ │ 调研中   │ │ 等待任务 │ │ 编码实现 │   │
│  │ 3min     │ │ WebSocket│ │          │ │ dashboard│   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘   │
│─────────────────────────────────────────────────────────│
│  📋 任务流水线                                          │
│  [调研]──→[方案]──→[审查]──→[编码]──→[验收]           │
│   Athena   Mercury  Bojack   Codex    Bojack            │
│   ✅done   ✅done   ◉doing   ○todo    ○todo             │
│─────────────────────────────────────────────────────────│
│  📜 活动时间线                                          │
│  ── 10:35 Bojack 审查了方案并提出修改意见               │
│  ── 10:33 Mercury 完成产品方案文档                      │
│  ── 10:32 Athena 完成调研，发现 4 个设计模式            │
│  ── 10:30 Bojack 派发调研任务给 Athena                  │
└─────────────────────────────────────────────────────────┘
```

## 3. 技术架构

- **单文件** HTML（内联 CSS + JS），零依赖，浏览器直接打开
- **CSS 变量** 驱动暗色主题
- **JS 数据驱动**：所有 Agent 状态、任务、活动日志从 JS 对象渲染
- **可选 WebSocket**：预留接口连接 web-ui 后端，但默认可离线演示

## 4. 配色方案（暗色主题）

| 用途 | 色值 | 说明 |
|------|------|------|
| 页面背景 | `#0A0A0F` | 最深底色 |
| 卡片背景 | `#1A1A2E` | 微紫黑 |
| 面板背景 | `#141428` | 介于中间 |
| 边框 | `#2A2A4A` | 半透明紫灰 |
| 文字主色 | `#E0E0F0` | 柔和白 |
| 文字次要 | `#8888AA` | 暗紫灰 |
| Bojack 强调色 | `#FFAB40` | 暖橙 |
| Athena 强调色 | `#00E5FF` | 青色 |
| Mercury 强调色 | `#B388FF` | 紫罗兰 |
| Codex 强调色 | `#69F0AE` | 翠绿 |
| 成功/完成 | `#4CAF50` | 绿色 |
| 进行中 | `#FFAB40` | 暖橙 |
| 空闲 | `#8888AA` | 灰色 |
| 脉冲动画 | `box-shadow` 呼吸效果 | 心跳指示器 |

## 5. 实现步骤（给 Codex）

1. **HTML 骨架 + CSS 变量系统** — 建立暗色主题基础设施，三区布局
2. **Agent 状态卡片** — 四张卡片含头像、状态指示灯（脉冲动画）、当前任务、耗时
3. **任务流水线** — 水平步骤条，连接箭头，状态着色
4. **活动时间线** — 垂直日志列表，图标+时间戳+事件描述
5. **数据绑定 + 模拟数据** — JS 对象驱动渲染，添加模拟协作场景数据

## 6. 文件结构

```
~/Documents/Hermes/workspace/opc-dashboard/
└── dashboard.html    (单文件，~500-800 行)
```
